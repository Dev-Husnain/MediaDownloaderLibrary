<?xml version="1.0" encoding="utf-8"?>
<!-- Permissions are added by the milestone whose feature needs them, never ahead of it. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Link parsing fetches pages and api answers; downloads fetch the media. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- A download waits for a connection instead of failing without one. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- A download keeps running with the app in the background, shown by its notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        Saving into Download/ with plain file access up to Android 10 (API 29): 29 still needs it,
        under the host's requestLegacyExternalStorage. Capped at 28 it merged over the host's own
        uncapped entry, and every download failed on Android 10. The host asks for it.
    -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application>
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
